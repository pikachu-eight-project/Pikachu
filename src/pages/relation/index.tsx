import styles from './index.module.scss'

const Relation = ()=>{
    return (
        <section className={styles['page-relation']}>
            <h1>联系我们</h1>
        </section>
    )
}

export default Relation;