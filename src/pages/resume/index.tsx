import styles from './index.module.scss'
const Resume = ()=>{
    return (
        <section className={styles['page-resume']}>
            <h1>简历</h1>
        </section>
    )
}

export default Resume;